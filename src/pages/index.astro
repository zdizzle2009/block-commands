---
import { ListView } from '../components/list-view';
import MainLayout from '../layouts/main-layout.astro';
import { getSortedEntries } from '../lib/get-sorted-entries';

const posts = await getSortedEntries()
---

<MainLayout>
	<ListView posts={posts} />
</MainLayout>
